import { ParentInterviewPreviewTool } from "@/components/parent-interview-preview/ParentInterviewPreviewTool";

export const metadata = {
  title: "Parent Interview Preview Tool",
  robots: {
    index: false,
    follow: false,
  },
};

export default function ParentInterviewPreviewPage() {
  return <ParentInterviewPreviewTool />;
}
